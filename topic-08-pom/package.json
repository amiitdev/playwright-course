{
  "name": "playwright-pom-topic-08",
  "version": "1.0.0",
  "type": "module",
  "description": "Topic 8 - Page Object Model (POM): classes for locators and actions",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
